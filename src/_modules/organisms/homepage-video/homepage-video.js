'use strict';

import $ from 'jquery';
import enquire from 'enquire.js';

export default class HomepageVideo {
  constructor(config) {
    this.config = config;
    let $header = $('header');
    let $headerH = $header.outerHeight();
    let $homepageVid = $('.homepage-video');
    let $videoHolder = $('.video-holder', $videoHolder);
  }
}
